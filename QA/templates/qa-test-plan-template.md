<!-- Template version: 9 (v5 added "Section F: Local-provider lookup entity disambiguation". v6 added "Section G: Temporarily-closed flag" and documented/fixed a Claude schema-union-limit bug on `contactInfo` (Methodology #15, Section G.5). v7 added "Section H: Settings entity" (Piece 3a) and documented/fixed a confabulation bug on a full-replacement array field. v8 added "Section I: Media Library and Screen entities" (Piece 3b) and documented/fixed the same confabulation pattern recurring on a full-replacement object field. v9 adds "Section J: Display Manager and Orders entities" after Piece 3c shipped (`displayManager` — machine rename + monitor screen assignment; `orders` — status only, no destructive-review special-casing since the entity contract's own `destructive` flag turned out to be dead code, never actually consulted by the real routing logic), and documents/fixes the *same* confabulation pattern a third time, on a plain single enum-of-live-names string field this time (`displayManager.assignedScreenName`) — broadening Methodology #15's rule once more: risk isn't about object/array shape, it's about any field grounded in live current-state context. Record which version a plan was written against in its own header, so a template change doesn't silently make old plans/reports look inconsistent with new ones.) -->

# Test plan: [what's being tested — e.g. "re-run the assistant QA pass on &lt;model&gt;, fresh-session-by-default"]

**Status:** Plan only — not executed. Do not run any Playwright/browser automation from this plan without asking first (per this repo's CLAUDE.md).
**Template version:** 9

**Source scenarios:** `[most recent report in QA/Reports/]` — reuse its own scenario IDs/phrasing verbatim, don't re-derive wording.

## Summary

*A short, scannable overview — a few bullets, written so someone can grasp the whole plan without reading the rest. Fill in before Methodology, not after.*

- **Model(s) under test:** [provider; thinking-role tag; vision-role tag if different, and why]
- **Session policy:** [fresh-per-scenario (default) / continuous / mixed — one line on why]
- **Environment:** [reusing existing seed fixture / re-seeding from scratch — current baseline counts if known]
- **Scope:** [N scenarios from the source report; any scenarios added/dropped/changed and why]
- **What changed since the last run:** [model, methodology, environment — whatever's actually different this time]
- **Headline things to watch for:** [1-3 bullets — a known weak spot from a prior run worth specifically re-checking, a new risk this model/config introduces, etc.]

## Methodology

*The durable, reusable rules — copy these forward from the last plan unless something genuinely needs to change. Don't re-litigate these from scratch each time.*

1. **What "fresh session" means mechanically.** "Fresh session" = calling the panel's own "New chat" button inside one continuously-running browser window — not relaunching Playwright/Chromium between scenarios. `localStorage`-backed settings (ingestion posture, per-chat model overrides, conversation log) are scoped to the browser profile, not the chat transcript, so they persist across "New chat" clicks. Confirm this is still true by re-reading `useLocalStorage`/`AssistantPanel`'s own state declarations if the assistant code has changed materially since the last run.
2. **Session-classification principle.** A scenario needs a shared/continuing session only when what's being tested is the model's own in-conversation memory or the chat UI's own not-yet-confirmed state — pronoun/reference resolution, reacting to the assistant's own last reply, answering a still-open clarification, correcting a just-proposed unconfirmed draft, per-card actions on an unconfirmed batch. A scenario that only needs a *real record to already exist* does not need a shared session — persisted app data outlives the session that created it. Classify every scenario against this principle by reading the source report's own text for it, not by guessing from the scenario's name — a batch/clarification/gate scenario usually stages *its own* fresh state rather than reusing a prior scenario's.
3. **Retry-once rule — use the app's own retry affordance when one exists, fall back to a fresh session when it doesn't.** There is exactly one in-app retry mechanism today: the Draft-Quality Gate's "Prøv igjen" button (`AssistantDraftQualityGate.tsx`, shown only under Safe/Automatisk posture once a draft has actually reached the gate) — it restores the original message into the composer, in the *same* session, for a real resend. When the failure is specifically "a draft reached the gate but its content looks wrong/low-quality," retry with that button first — it's the faithful, same-session retry a real admin would use. When the failure is that nothing ever reached a testable state at all (no gate, no clarification, no batch, an empty reply, a timeout), there's no button to click — the precondition itself never fired — so retry by starting a fresh session and resending the same message instead. Either way, keep both attempts' notes in the report and only mark the scenario as failed-to-reach-that-state if the retry (whichever kind) also comes up empty.
4. **Diagnostic addendum (optional, doesn't affect grading).** After the fresh-per-scenario pass, optionally re-run a handful of Section A scenarios back-to-back in one continuous session, purely to check whether this model shows the kind of mid-session tool-call degradation a prior run may have documented for a different model. Label it clearly as not affecting any scenario's PASS/FAIL grade.
5. **Cleanup policy.** Keep the seed fixture in place at the end (don't tear it down) so a future re-test doesn't need to re-seed from scratch. Only clean up records individual scenarios created on top of it. Verify every "this was created" claim against real data (`server/data/*.json` or the admin UI) before writing it into the cleanup table — a script's own "confirmed=true" flag can be a false positive if it only checked that a review panel existed, not that clicking Confirm actually persisted (a disabled button click is a silent no-op).
6. **Screenshot retention.** Once this run's report is finished and its own screenshot folder (`QA/assistant-qa-screenshots-<label>/`) is in place, delete the *previous* test cycle's screenshot folder(s) — keep only the most recent test's screenshots, to save disk space. This will leave older reports' own screenshot links dead; that's an accepted tradeoff (the report text stands on its own) rather than an oversight. Don't delete the previous cycle's report `.md`/plan/prompt files themselves, only the image folders.
7. **Harness-vs-reality cross-check.** The Playwright harness grades most scenarios by reading the DOM (`innerText`, element counts) — trust that as a first pass, not a final verdict. For any scenario the harness marks FAIL where the trace shows the underlying pipeline actually fired correctly (right filter, right entity, right candidate), open the real screenshot before writing the grade down. The harness has already been wrong once this way (a list rendered correctly on screen while a naive `innerText` read of only the first line missed it). If the screenshot disagrees with the harness's own read, the screenshot is truth.
8. **Root-cause consolidation, not just a symptom count.** When 2+ scenarios show the same underlying failure (e.g. "records keep landing in the wrong catalogue"), don't report them as N separate findings — name the suspected single root cause in the Key Findings section and list every scenario ID that confirms it. This is what actually tells a reader whether follow-up work is one fix or several — see the report template's own Key Findings instructions.
9. **Known-deferred-scenario carve-out.** Some scenarios test functionality that hasn't shipped yet as of this run's own baseline (check the assistant code's own phase/plan comments, or the source report's own "out of scope"/"diagnostic only" scenarios). List these explicitly (see "Known deferred scenarios" below) and grade them **N/A — pending [feature/phase]**, not FAIL — a report's tally should never make known future work look like a regression.
10. **Phase attribution.** If the assistant code is being developed in labeled phases (check `server/assistant/*` and prior reports for phase references), tag each scenario in the classification table with which phase it tests. This is what lets a report answer "did anything *shipped* regress?" separately from "what's still pending?" as the phase list grows — a scenario failing could mean the model can't do it, an earlier phase broke, or a later phase simply hasn't landed.
11. **Execution mode.** Headed (visible) Chromium, not headless.
12. **Known environment gotchas** — carry forward anything discovered in a prior run that will recur: [e.g. iCloud Drive sync conflict-duplicating `QA/scratchpad/` mid-run under heavy concurrent writes — sync noise, not data loss, don't panic-diagnose as a script bug; a stray substring-collision false-positive in a name-`contains` filter; etc. Delete this bracket and list what's actually still relevant.]
13. **Reuse `QA/scratchpad/qa/harness.mts` as-is before writing new scenario helpers.** It already carries forward every harness-level fix found across prior cycles (see below) — copy it into the new run's own script alongside a fresh `retest-<model>.mts`, don't hand-roll a page-object layer from scratch. Diff it against `QA/scratchpad 2/qa/harness.mts` (an older, pre-fix iCloud-sync-duplicate copy) if you're ever unsure which is current — the one under plain `QA/scratchpad/qa/` is authoritative.
14. **Known harness fixes already applied — don't re-discover these:**
    - `login()` now calls `setDashboardLanguage(page, 'Norsk')` itself. A fresh browser context otherwise defaults to English, and every catalogue/category name this harness matches against (`AutoDeler`, `Dekk`, `Bremser`, ...) is Norwegian-only text with a blank English variant — untangling a script that silently times out on every single catalogue lookup because of this cost real time in the qwen3:8b cycle before the actual cause (missing language default, not a real app issue) was found. Only call `setDashboardLanguage(page, 'English')` explicitly for the handful of scenarios that specifically test the English UI, then switch back.
    - `sendChat`'s own trace capture now reads the assistant panel's admin-only "Copy conversation to clipboard" export (`captureLatestTraceViaClipboard`) instead of clicking through every collapsible thought/step row in the DOM. This is both faster and more reliable — the export is built from React state, not the rendered DOM, so it includes every step regardless of expand/collapse state, with no risk of a click racing a still-animating transition. Each field is still capped at 500 characters server-side, same as the old DOM read, so nothing is lost. The old click-through version is kept as `captureLatestTraceDOM` for reference only — no current script should call it.
    - `sendChat`'s own `reply` now also captures the sibling `.assistant-list-attachment` element, not just the text bubble — a "list all products"/bilingual-filter-style reply's actual item list renders as a *sibling* of the bubble, not nested inside it, so reading only the bubble text reliably misses every item (this produced a false "0/3 found" reading for A.9a/A.9b in the qwen3:8b cycle, corrected only by a manual screenshot check).
    - A new `productExistsInCategory(page, categoryName, productName)` helper properly expands the category section before checking — a raw `.products-view__item-name` count on a still-collapsed section reads 0 even when the product is really there, which caused a scenario to force-create a real duplicate product in the qwen3:8b cycle. Use this for any "seed the target if a prior scenario's create didn't land" fallback instead of rolling an ad hoc check.
    - A new `reachBatchReview(page)` helper checks for the Draft-Quality Gate first and clicks "Se detaljer" before checking for batch-review cards. Checking `isBatchReviewVisible` directly, without first opening a gate that might be sitting in front of it, produced a false `batch=false` reading across six different scenarios (three different entity types) in the qwen3:8b cycle — the model had staged valid batches every time. Use `reachBatchReview` for any scenario that might produce more than one record, rather than a raw `isBatchReviewVisible()` check after `sendChat`.
15. **Known, carried-forward app-level findings (not harness bugs) — reproduce and cite, don't re-diagnose from scratch unless this run's own trace looks meaningfully different:**
    - **Chat-driven update/delete via search stalls silently after correctly resolving the target item** (`select_command`/`select_item` fire correctly, then no reply/review/destructive-confirm UI ever appears) — confirmed across two different models (normistral-it:7b, qwen3:8b) on the exact same two scenarios (update price, delete product), including with zero candidate ambiguity. Strong signal this is a real, model-independent app bug, not a model-quality issue. If a given cycle finds this fixed, that's a genuine, headline-worthy positive — say so explicitly.
    - **Confirming one card in a multi-card batch review discards the other, still-unconfirmed cards instead of leaving them independently actionable** — reproduced twice in the qwen3:8b cycle (once mid-run, once in an isolated clean retry). The bulk "Bekreft alle"/"Avbryt alt" actions are unaffected (they deliberately act on the whole batch). If testing per-card confirm/edit/remove independence, check `batchCardCount` before and after the first single-card action to confirm whether this is still happening.
    - **A UI label was renamed from "Diettmerker" to "Kosthold-tagger"** (dietary-tags field, single-record review form) — any check written against the old label text will silently false-negative. Confirm the current label text directly from a screenshot before writing a new automated check against it, rather than trusting a prior report's own wording.
    - **FIXED (was: `contactInfo`'s `update` action completely broken on the Claude provider specifically).** `fillFieldsSchema()` for this entity had grown to 33 nullable/`anyOf`-typed properties (3 phone/email/address + 2 temporarilyClosed/temporarilyClosedReason + 7 per-weekday wrapper objects + 21 nested closed/open/close fields) — Claude's API rejects any single tool schema with more than 16 such union-typed parameters (`400 invalid_request_error`, "Schemas contains too many parameters with union types... exponential compilation cost"). Confirmed pre-existing (31 unions, already double the limit, before the temporarily-closed fields were added — not a regression from that work); the local/Ollama provider was never affected. Fixed in the same change that added `temporarilyClosed`/`temporarilyClosedReason`: each per-weekday day object's own `closed`/`open`/`close` fields are no longer individually `nullable(...)` — only the day's own wrapper object is (skip a day the message doesn't address), and `open`/`close` use `""` instead of `null` for "not applicable" inside a touched day. Brings the total to 12 union-typed properties, confirmed via a real Claude-provider round trip (set + clear `temporarilyClosed` via chat, review card rendered correctly, write persisted). If a future cycle finds this regressed (an entity's schema creeping back over 16 unions as more fields get added), that's worth flagging explicitly rather than silently re-working around it per-field.
    - **STANDING RULE (confirmed three times now, broader than first thought): confabulation-risk isn't limited to "full replacement compound object/array" fields — a single nullable enum-of-live-names STRING field hit the same failure.** First confirmed on `settings.hiddenSidebarItems` (array): asked only to switch the clock format, qwen3:8b filled it with all 7 sidebar items, which would have hidden the entire sidebar. Confirmed again on `screen.textSizes` (object): asked only to turn on pane borders, qwen3:8b fabricated a whole all-zero `textSizes` object. Confirmed a third time, independently, on `displayManager.assignedScreenName` (a plain string, not a compound value at all): asked only to rename a machine, qwen3:8b reassigned one of its monitors from its real current screen ("Screen 3") to a different real screen name ("Skjerm 2") — not a placeholder guess, a genuine fabricated change to live state, verified by cross-checking the actual persisted `assignedScreenID` before asserting this was the model's fault and not a bug in the entity's own current-value resolution. All three fixed via `AssistantEntity.confabulationRiskFields`, stripping the field from the schema entirely under `'safe'`/local-default posture; all three re-tested clean after their fix. **The common thread isn't "object/array" — it's a field whose valid values are grounded in live current-state context** (what's currently hidden, what a pane's current sizes are, what a monitor currently shows) **rather than a small, fixed, context-free enum** (like `clockFormat`'s `'24h'`/`'12h'`, which has never shown this problem). Treat any new field matching that shape as confabulation-risk by default, regardless of whether it's scalar or compound — don't wait for its own repro before adding it to `confabulationRiskFields`.
    - **`AssistantEntity.destructive` and `.reviewComponent()` are both dead code — declared in the contract, set per-entity by every existing entity file, but never actually consulted by the real routing/rendering logic.** Confirmed by grepping the entire codebase for calls to either: zero hits outside their own declarations. The real decision of whether a chat action shows the typed-confirmation destructive UI is hardcoded to a literal `action === 'delete'` check in `useAssistantFlow.ts` (`proceedWithItem`), completely independent of what any entity's own `destructive(action)` returns — and that destructive UI's own copy is itself hardcoded to the word "Delete," not phrased generically for other irreversible actions. This means setting `destructive: (action) => action === 'update'` (or any non-delete action) on a new entity compiles cleanly and looks like it does something, but has **zero actual effect** — don't trust that field's presence as evidence a given action gets extra confirmation friction; check `useAssistantFlow.ts`'s own literal action checks instead. `orders.ts` deliberately does NOT set a `destructive` field for this reason — order status changes go through the same plain review-and-confirm flow as every other `update`, not a special stricter one, since building real support for a non-delete destructive flow (a client-side signal plus action-aware confirmation copy) was scoped out as a separate future change, not bundled into adding the entity.
16. **Concurrent-turn safety scenarios (Section E) need `sendChatConcurrent`, not `sendChat` twice in a row.** Every other scenario in this template calls `sendChat` (or a helper built on it) once per turn and awaits full completion before the next call — Section E is the one deliberate exception, since it exists specifically to test what happens when a second turn starts before the first has resolved. Use `sendChatConcurrent(page, message1, message2, { interrupt })` (`QA/scratchpad/qa/harness.mts`) for E.1–E.2, with `interrupt: 'cancel'` / `'newChat'` matching which affordance the scenario is testing. E.3 (the Draft-Quality Gate's own "Prøv igjen" retry) needs its own staging to actually reach the gate first — drive it by hand with `clickGateTryAgain` rather than through `sendChatConcurrent`. Assert on both halves of `sendChatConcurrent`'s own return value: `reply`/`trace` (message 2's own result — must be exactly what the second message alone would have produced, uncorrupted by the first) and `staleLines` (should be non-empty for a genuinely-raced turn, confirming the discard is visible in the transcript, not silently dropped per Phase A's own design). E.4–E.5 are Phase-B-only and largely not harness-automatable (E.4 needs a Network-panel read, E.5 needs a GPU-usage read) — grade those manually per the scenario's own instructions rather than trying to script an assertion for them.

## Known deferred scenarios

*Scenarios in the source report's own list that test functionality not yet shipped as of this run's baseline. They still run for diagnostic value — worth knowing whether a not-yet-shipped feature is at least trending toward working — but are graded N/A-pending, not FAIL, in the final report.*

| ID | Pending on | Why it still runs |
| --- | --- | --- |
| [ID] | [feature/phase name] | [what diagnostic value it has anyway, if any — delete the row if it has none and should just be skipped] |

## Model & vision configuration

- **Thinking model:** `[exact installed tag]`, set via Integrations' "Custom" field (or preset tier if one exists for it).
- **Vision model:** `[exact installed tag]` — confirm capability first: `ollama show <tag>` should list `vision` under Capabilities before assuming a tag can serve this role. If the thinking-role tag can't also serve vision, decide explicitly (state the choice here, don't silently skip) whether the vision-dependent scenario runs against a different real vision-capable model, or is marked out of scope.
- **Scope note:** [does any scenario in this run's scenario list actually exercise an image-attach flow? If not, say so — the vision choice is configuration-only and doesn't affect scenario selection.]
- **Not yet supported by this template:** testing the same scenario battery across multiple model tiers in one cycle (a matrix rather than a single linear result). Today's templates assume one model configuration per cycle; if/when there's a need to check "does the current build work across every model tier we support," that's a real extension to design for deliberately rather than bolt on ad hoc — don't try to force it into the single-model structure below.

## Environment setup (prerequisite, before any scenario runs)

- [Re-seed via `QA/scratchpad/qa/seed.mts` if the fixture doesn't currently exist — or confirm it does and skip.]
- [Re-measure the actual current baseline right after seeding/confirming — don't trust hardcoded numbers from a prior report; environment drift between runs is expected.]
- [Any unresolved artifact from a prior report worth checking before this run, so it isn't rediscovered mid-grading and mis-attributed to this model.]

## Section E: Concurrent-turn safety (standing scenario bank)

*Unlike every other scenario in this template, these aren't sourced from the most recent report — they test the app's own turn-versioning/cancellation engineering (`useAssistantFlow.ts`'s `turnVersionRef`/`abortRef`, `server/index.ts`'s per-route `AbortController`), not model behavior, so they're defined once here and reused verbatim across cycles rather than re-derived from a prior run. Include them in a cycle's own classification table below whenever concurrent-turn-safety work has shipped or changed since the last cycle that covered it — otherwise they can be skipped entirely (that's not the same as failing them).*

*Vary the example messages' own phrasing and verb across rows — don't default to "Legg til..." ("add...") for every one. Real admins phrase the same intent differently ("Sett opp..." / "set up...", "Opprett..." / "create...", "Lag..." / "make...", "Registrer..." / "register...", "Start...", or a plain factual question rather than a command at all), and this repo's own QA findings have already shown model behavior can be phrasing-sensitive — a battery that only ever exercises one verb tests less than it looks like it does. The pairs below are a starting set, not the only acceptable one; keep the variety when substituting a different pair for a specific cycle's own seed data.*

| ID | Scenario | Example messages | Phase | Session |
| --- | --- | --- | --- | --- |
| E.1 | Send message 1, interrupt via Cancel before it resolves, send message 2, let both resolve. Assert message 2's own reply/trace came back clean and `staleLines` (from `sendChatConcurrent`) is non-empty. | 1: "Sett opp en ny ansattbruker som heter Kari Nordmann" (set up a user) · 2: "Hvor mange produkter er på tilbud akkurat nå?" (a lookup question, not a command — confirms the guard holds across different message *types*, not just two commands of the same shape) | Phase A | Shared (E.1 continues into E.2/E.3 within the same chat unless noted) |
| E.2 | Same shape as E.1, but interrupt via "New chat" instead of Cancel. | 1: "Opprett et arrangement som heter Julebord, 15. desember" (create an event) · 2: "Registrer en ny produktvariant, Iskaffe til 45 kr" (register a product) | Phase A | Fresh (New chat resets it anyway) |
| E.3 | Stage a draft to the Draft-Quality Gate, retry via "Prøv igjen" (`clickGateTryAgain`), then send a second, different message before the retry's own response lands. Needs custom staging — not drivable through `sendChatConcurrent` directly (see Methodology #16). | 1 (staged, then retried unchanged): "Lag en ny kategori som heter Sommerdrikker" (make a category) · 2 (sent before the retry resolves): "Sett opp kontaktinfo med telefonnummer 12345678" (set up contact info) | Phase A | Shared |
| E.4 | With the browser's Network panel open, interrupt an in-flight turn via Cancel and confirm the request shows `(canceled)` rather than completing. Manual read, not harness-asserted. | "Start en oppdatering av prisen på Nachos til 110 kr" (a plain update, phrased as "start...") | Phase B | Fresh |
| E.5 | On a local Ollama model, interrupt a mid-generation turn and confirm (`nvidia-smi` or platform equivalent) the model frees up for a follow-up turn within ~1s rather than waiting for the abandoned generation. Manual read, not harness-asserted, no DOM signal to script against. | "Legg til en ny produktvariant som heter Grønn Smoothie, 60 kr" (the one row that's allowed to actually use "Legg til...", since every other row already covers a different verb) | Phase B | Fresh |

## Section F: Local-provider lookup entity disambiguation (standing scenario bank)

*Same posture as Section E — defined once here, not sourced from a prior report, and pulled into a cycle's own classification table whenever `selectLookupTarget`/`selectIntentCascaded` in `server/assistant/steps.ts` (the local-provider intent cascade) has been touched since the last cycle that covered it. Motivating bug: `selectLookupTarget`'s own system prompt carried no entity descriptions (unlike `selectIntentSinglePass`'s `entityListLine(...)`), so a local model choosing which entity answers a factual question had nothing but bare enum keys to go on — confirmed picking `storeSettings` (no hours field) over `contactInfo` (has `hours`) for "when do you close," and answering "not in the data" as a result. Fixed by threading `entityListForPrompt` through to `selectLookupTarget` the same way the Claude path already had it. These scenarios test entity **selection** specifically — whether `lookupEntities` in the trace resolves to the right entity key — not the composed reply's own factual accuracy (a separate, model-quality concern; note it in Key Findings if observed, but it doesn't affect this section's own PASS/FAIL).*

| ID | Scenario | Example messages | Phase | Session |
| --- | --- | --- | --- | --- |
| F.1 | Ask a factual question whose answer lives on an entity distinguishable only by its description, not its bare key name (`contactInfo` vs `storeSettings` — both plausible-sounding for "store info"). Assert (via trace) that `lookupEntities` resolves to the entity that actually has the field asked about, not merely that a reply arrived. | "Når er stengetid?" / "When do you close?" (needs `contactInfo`'s `hours`, not `storeSettings`) | Shipped (local-lookup fix) | Fresh |
| F.2 | Same question, different phrasing, to confirm the fix isn't phrasing-specific. | "Hva er åpningstidene deres?" / "What are your opening hours?" | Shipped (local-lookup fix) | Fresh |
| F.3 | Regression check on the already-working Claude path — confirm `selectIntentSinglePass` is unaffected by the `selectLookupTarget` change (they're separate functions, but worth confirming the fix didn't touch shared state). | "Når er stengetid?" (same as F.1, run against Claude instead of the local provider) | Shipped (local-lookup fix) | Fresh |

## Section G: Temporarily-closed flag (standing scenario bank)

*Same posture as Sections E/F — defined once here, pulled in whenever `ContactInfo.temporarilyClosed`/`temporarilyClosedReason` (`src/types/contactInfo.ts`) or `contactInfoEntity`'s handling of them (`server/assistant/entities/contactInfo.ts`) has changed since the last cycle that covered it. G.1–G.4 run on either provider now that the Claude schema-limit bug (Methodology #15) is fixed — G.5 specifically re-confirms the Claude path, since it's the scenario that would catch a regression if a future field addition pushes the schema's union count back over Claude's 16-property limit.*

| ID | Scenario | Example messages | Phase | Session |
| --- | --- | --- | --- | --- |
| G.1 | Set the flag on with a reason via chat, confirm the review card shows both the toggle and reason changing, confirm it persists to `server/data/admin-contactInfo.json` after confirming. | "Vi er midlertidig stengt i dag for et privat arrangement, sett dette i kontaktinfo." (we're temporarily closed today for a private event) | Shipped (temporarily-closed flag) | Fresh |
| G.2 | With the flag on, ask a plain hours question and confirm the reply leads with the closure (and reason) rather than reciting the regular weekly hours as if nothing changed — this is the `lookupGuidance` read-side behavior, not just a write check. | "Når har dere åpent?" / "When are you open?" | Shipped (temporarily-closed flag) | Fresh |
| G.3 | Clear the flag via chat, confirm the review card shows it flipping back off and the reply to a following hours question reverts to the normal weekly schedule. | "Skru av midlertidig stengt i kontaktinfo." (turn off temporarily closed) | Shipped (temporarily-closed flag) | Fresh |
| G.4 | Manual UI check (not chat): toggle the flag and reason via Store settings → Contact info directly, confirm it persists across a reload, and confirm the reason input only appears while the toggle is checked (same conditional-field pattern as a weekday's own open/close inputs). | N/A — manual UI interaction, not a chat message | Shipped (temporarily-closed flag) | N/A |
| G.5 | Claude-provider regression check for the schema-union-count fix (Methodology #15): run G.1's own scenario against Claude instead of the local provider, confirm `fill_fields_contactInfo` completes without a `400` schema error and the review card renders correctly. | "Sett kontaktinfo til midlertidig stengt, årsak: [any short reason]." | Shipped (temporarily-closed flag) | Fresh |

## Section H: Settings entity (standing scenario bank)

*Same posture as Sections E/F/G — defined once here, pulled in whenever `server/assistant/entities/settings.ts` or its client-side wiring (`useAssistantFlow.ts`/`AssistantPanel.tsx`'s `SettingsMiniForm`) has changed since the last cycle that covered it. H.2 specifically guards against a real, found-and-fixed confabulation bug (Methodology #15) — treat any regression there as high-severity, not a routine model-quality miss, since it silently broke the entire admin sidebar in real testing.*

| ID | Scenario | Example messages | Phase | Session |
| --- | --- | --- | --- | --- |
| H.1 | Change one settings field via chat, confirm the review card shows only that field changing (not every field the model could theoretically touch), and confirm it persists to the real Settings page after confirming. | "Bytt til 12-timers klokke i innstillingene." (switch to 12-hour clock) | Shipped (settings entity) | Fresh |
| H.2 | **Confabulation regression check.** Send a settings message that names one field only, then inspect the raw `fill_fields_settings` trace output directly (not just the rendered review) to confirm `hiddenSidebarItems` is either `null` or absent entirely — never a fabricated full list — on the local provider. If this regresses, treat it as a real, high-severity finding (it silently hid the entire sidebar in the run that first found it), not routine model noise. | "Bytt datoformat til måned/dag/år." (switch date format to month/day/year) | Shipped (settings entity) | Fresh |
| H.3 | Hide one specific sidebar item via chat on **Claude** (the local provider can't touch `hiddenSidebarItems` at all post-fix — it's stripped from that provider's schema, see Methodology #15). Confirm the resulting list contains exactly the previously-hidden items plus the new one, not every possible item. | "Skjul Bestillinger-fanen fra sidemenyen." (hide the Orders tab from the sidebar) | Shipped (settings entity) | Fresh |
| H.4 | Manual UI check (not chat): change the kiosk pane default language via Settings directly, confirm it syncs live to a second open browser tab (proving the real synced-key path, not a stale per-tab value) — this is what confirms `clockFormat`/`dateFormat`/`paneLanguage`/`sidebarSettings` are genuinely `SYNCED_KEYS` and not device-local, per the entity's own design rationale. | N/A — manual UI interaction across two tabs, not a chat message | Shipped (settings entity) | N/A |

## Section I: Media Library and Screen entities (standing scenario bank)

*Same posture as Sections E–H — defined once here, pulled in whenever `server/assistant/entities/mediaLibrary.ts`/`screen.ts` or their client-side wiring (`useAssistantFlow.ts`/`AssistantPanel.tsx`'s review-mount and destructive-delete branches, `reviewChangeRows.ts`'s `buildMediaLibraryChangeRows`/`buildScreenChangeRows`) has changed since the last cycle that covered it. I.4 guards against the same confabulation pattern as Section H.2 — recurring on a second, independent entity is exactly what promoted it from a per-field fix to the standing Methodology #15 rule, so treat any regression here as equally high-severity. I.2/I.3 specifically test the `screen` entity's own scope boundary (global settings only, never pane layout/content) — the one place in this whole registry expansion where scope creep would be easy to miss, since `ScreenForm.tsx` is reused directly rather than a purpose-built mini-form.*

| ID | Scenario | Example messages | Phase | Session |
| --- | --- | --- | --- | --- |
| I.1 | Rename a media file via chat, confirm the review shows the old filename/label → the new label, and confirm the new label shows on the real Media Library page after confirming. | "Gi filen [existing filename] merkelappen \"[new label]\"." | Shipped (mediaLibrary entity) | Fresh |
| I.2 | Create a screen via chat naming only its title, confirm the review/edit form is the real `ScreenForm` (not a mini-form), and confirm every unaddressed field defaults sanely (single stage, 10s duration, fade transition, no borders) — critically, confirm the created screen has a valid single-pane layout showing a live clock, not a broken/empty arrangement. | "Lag en ny skjerm som heter \"[name]\"." | Shipped (screen entity) | Fresh |
| I.3 | Update one specific global setting on an existing screen via chat (e.g. turn on pane borders), then inspect the real screen's own `layout`/`paneSlots` directly (`server/data/admin-screens.json` or the admin UI) to confirm they're byte-identical to before — this is the scope-boundary check: a chat-driven global-settings update must never touch pane structure/content, only the entity's own explicitly-scoped fields. | "Skru på synlige kantlinjer på skjermen \"[name]\" og sett kantfargen til #ff0000." | Shipped (screen entity) | Fresh |
| I.4 | **Confabulation regression check**, mirroring H.2 but for `screen`. Send an update naming one field only, then inspect the raw `fill_fields_screen` trace output directly to confirm `textSizes`/`backgroundImage` are either `null` or absent entirely on the local provider — never a fabricated object (e.g. an all-zero `textSizes`). | "Bytt overgangsstil på skjermen \"[name]\" til slide." (switch transition style to slide) | Shipped (screen entity) | Fresh |
| I.5 | Delete a screen via chat, confirm the typed-confirmation destructive flow (same shared mechanism every other deletable entity uses) resolves the right target by name and actually removes it from the real Screens list after confirming. | "Slett skjermen \"[name]\"." | Shipped (screen entity) | Fresh |
| I.6 | Delete a media file via chat, confirm the same typed-confirmation flow, and confirm the file is actually gone from the Media Library page after confirming. | "Slett mediefilen [filename]." | Shipped (mediaLibrary entity) | Fresh |

## Section J: Display Manager and Orders entities (standing scenario bank)

*Same posture as Sections E–I — defined once here, pulled in whenever `server/assistant/entities/displayManager.ts`/`orders.ts` or their client-side wiring has changed since the last cycle that covered it. J.2 is the third confirmed instance of the standing confabulation rule (Methodology #15) — a regression here means the rule itself needs re-examining, not just this one field. J.3 covers the three-way order commit-path split (website/Wolt/Foodora) — the one place in this whole registry expansion where a wrong dispatch would silently push a status update to the wrong system (or none at all) while still looking successful in the chat UI.*

| ID | Scenario | Example messages | Phase | Session |
| --- | --- | --- | --- | --- |
| J.1 | Rename a display machine via chat, confirm the review shows the old label → the new one, and confirm it shows on the real Display Manager page after confirming. | "Gi skjermtilkoblingen \"[machine label]\" navnet \"[new name]\"." | Shipped (displayManager entity) | Fresh |
| J.2 | **Confabulation regression check.** Rename a machine via chat (naming only the machine, not any monitor/screen), then inspect the raw `fill_fields_displayManager` trace output directly to confirm `assignedScreenName` is `null`/absent on the local provider — never a fabricated reassignment. If this regresses, cross-check the real `assignedScreenID` before/after against `server/data/admin-displayMachines.json` (not just the rendered review) to confirm it's a genuine fabrication and not a display bug in the review row itself. | "Gi skjermtilkoblingen \"[machine label]\" navnet \"[new name]\"." (same message as J.1 — the point is confirming it does NOT also touch the screen assignment) | Shipped (displayManager entity) | Fresh |
| J.3 | Assign a screen to a monitor via chat on **Claude** (the local provider can't touch `assignedScreenName` at all post-fix — stripped from that provider's schema, see Methodology #15). Confirm the resulting assignment matches the named screen exactly, not a different one. | "Sett skjermen \"[screen name]\" på \"[monitor label]\"." | Shipped (displayManager entity) | Fresh |
| J.4 | Change a **website** order's status via chat, confirm it persists to `admin.orders` (the plain synced-key path, no external push). | "Sett bestillingen til [customer name] til status [status]." | Shipped (orders entity) | Fresh |
| J.5 | Change a **Wolt** order's status via chat, confirm the review resolves the right order (search by customer name/phone works even though the order's own `id` is an internal, not customer-facing, value), confirm the local `admin.woltOrders` entry updates optimistically, and confirm the real push attempt fires (`pushWoltOrderStatus` — expect it to fail cleanly with no real Wolt credentials configured in a dev environment; that's a correct, safe result, not a bug to chase). | "Sett bestillingen til [Wolt customer name] til status klar." | Shipped (orders entity) | Fresh |
| J.6 | Change a **Foodora** order's status via chat — same shape as J.5, confirming the Foodora-specific push path fires instead of Wolt's. | "Sett bestillingen til [Foodora customer name] til status klar." | Shipped (orders entity) | Fresh |
| J.7 | Manual UI check (not chat): confirm `orders.ts`'s own review has no typed-confirmation step (per the dead-`destructive`-field finding in Methodology #15) — a status change should reach the normal single-click-confirm review, not a "type to confirm" prompt. This isn't a regression to fix if it holds; it's confirming the deliberate scope decision stayed in place. | N/A — confirming the *absence* of a UI element, not a chat message | Shipped (orders entity) | N/A |

## Full scenario classification

*One row per scenario ID from the source report, reused verbatim, plus any Section E rows this cycle is including (see above — those aren't from the source report). Fresh vs. shared + a one-line reason citing the principle above. `Phase` is which shipped phase (or "pending") the scenario actually tests — see Methodology #9 and the Known Deferred Scenarios list above; leave the column out entirely if this project isn't using phase labels.*

| ID | Session | Reason | Phase |
| --- | --- | --- | --- |
| [ID] | Fresh / Shared (with which) | [one line] | [e.g. Phase 2 / pending] |

## Run order

*Any order is fine for plain "Fresh" scenarios. Call out explicitly:*
- Any genuine **data** dependency (not session) — e.g. an update/delete scenario needs an earlier create scenario's own record to actually exist; note the fallback if that create doesn't succeed this time (seed the target manually so the later scenario can still be graded on its own merits).
- Any ordering needed to read one scenario's result in light of another's (e.g. run the "does the fallback mechanism work at all" scenario before the "does it work in this other language/direction" one).

## Report format

Follow `QA/templates/qa-test-report-template.md`. Same PASS/PARTIAL/FAIL/N/A/ERROR status vocabulary, trace notes, screenshot reference, key-findings summary, and cleanup table as prior reports, so results stay comparable across runs.

## Verification

- Every ID from the source report's own scenario list appears exactly once in the classification table above.
- The model/vision decision, environment setup, and report-format sections are all filled in, not left as brackets.
- Any scenario testing not-yet-shipped functionality is listed under Known Deferred Scenarios, not silently left to fail the tally.
- Nothing in this plan tells anyone to execute Playwright/browser automation without asking first.

## When to run this cycle

Event-driven — after a labeled phase of assistant work lands, or when evaluating a new model worth benchmarking for a supported tier. Not a fixed schedule.
